/**
 * Values are also used in [`index.html`](../../index.html).
 *
 * Defaults to browser preference if one can be found; otherwise {@linkcode AppTheme.light}.
 */
export enum AppTheme {
  light = "light",
  dark = "dark"
}